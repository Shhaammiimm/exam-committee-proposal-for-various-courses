PORT=5000
# MongoDB Atlas connection string (get from Atlas dashboard: Connect -> Drivers -> Node.js)
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/exam-committee?retryWrites=true&w=majority
# Alternative env name some hosts use:
# ATLAS_URI=mongodb+srv://...
